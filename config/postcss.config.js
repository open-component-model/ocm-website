// Minimal PostCSS config to satisfy Hugo's PostCSS transform.
// Doks and theme assets will work without additional plugins.
// If needed later, add plugins like 'autoprefixer' here.
module.exports = {
  plugins: []
};